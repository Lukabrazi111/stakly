# Changelog

Notable changes to Stakly, one entry per shipped version. Versioning is
`MAJOR.MINOR.PATCH.MICRO` (the gstack `/ship` convention); this file is
auto-maintained by `/ship`. Product / roadmap tracking lives in `milestones.md`
(active) and `milestones_archived.md` (shipped).

## v0.1.0.0 — 2026-07-04

- Adopt the gstack `/ship` scaffolding (`VERSION` + this changelog) so `/ship`
  can run the full test → review → version-bump → push → PR pipeline. Existing
  feature history (M1–M42) lives in `milestones_archived.md`.
